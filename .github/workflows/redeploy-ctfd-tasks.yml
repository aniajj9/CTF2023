name: Run CTFd Script

on:
  push:
    branches:
      - main

jobs:
  run-script:
    name: Run ctfd.py
    runs-on: ubuntu-latest

    steps:
      - name: Check out code
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install dependencies (if needed)
        run: pip install -r requirements.txt

      - name: Run ctfd.py
        run: python ctfd.py ./ --token 5e5548a2f18a6458fb2c24f470116f98ff32c412d25f8e9f6ee10e0b5a51af87 --url http://ctfd-app-wph3mgffdp5qm.azurewebsites.net
