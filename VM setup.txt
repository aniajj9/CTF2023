1. Setup Docker on VM:
	a. Install Docker
	b. Install Docker-compose (via PIP!)
	c. Set up Docker to start automatically (sudo systemctl start docker  # On systems using systemd)
2. Build all docker-compose files (run python3 docker-compose-recurse.py)


sudo usermod -aG docker <your-username>


sudo apt-get install python3-pip
pip install --upgrade pip
pip install docker-compose


git config --global core.autocrlf input
git config --global core.eol lf


make all .sh files executable:
 find CTF2023 -type f -name "*.sh" -exec chmod +x {} \; 